 I am learning how algorithms are structured. Key components include:

1.Input/Output
num = int(input("Enter a number: ") //input
square = num * num //process
console.writeline("The square is:" + square) //output
2.Conditionals
--Sequential Execution: Steps execute in order.

--Selection (Decision Making): if, if-else, switch statements.

== Example: if num > 0: console.writeline("Positive")
else
{
console.writeline("Negative");
}
3.Loops
Iteration (Loops): for, while, do-while loops.
for loop : for(init; condition; process;)
4.Functions
FOR LOOP IN sorting arrays code:
for (int i = 0; i < arr.Length - 1; i++) {
    for (int j = 0; j < arr.Length - i - 1; j++) {
      if (arr[j] > arr[j + 1]) {
        int temp = arr[j];
        arr[j] = arr[j + 1];
        arr[j + 1] = temp;
      }
}
